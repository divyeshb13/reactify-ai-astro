---
interface Props {
  className?: string;
}

const { className = "" } = Astro.props;
---

<div class={`absolute bottom-0 left-0 right-0 w-full ${className}`}>
  <div
    class="relative h-px overflow-hidden bg-[image:var(--gradient-primary)] opacity-30"
  >
  </div>
</div>
