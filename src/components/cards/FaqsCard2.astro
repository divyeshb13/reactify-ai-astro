---
import { Sparkles } from "lucide-react";
import CardGradientBg from "@/components/background_animation/CardGradientBg.astro";
---

<div
  class="bg-card/50 backdrop-blur-xl border border-primary-foreground/10 sm:p-8 p-5 rounded-3xl overflow-hidden relative flex flex-col gap-6 items-center justify-center group"
>
  <CardGradientBg className="opacity-0 dark:opacity-10" />
  <div
    class="sm:w-20 sm:h-20 w-14 h-14 rounded-full bg-primary flex items-center justify-center mx-auto mb-2 relative z-10"
  >
    <Sparkles className="size-8 sm:size-10 text-white" />
  </div>

  <h3
    class="text-2xl sm:text-3xl font-bold bg-primary bg-clip-text text-transparent text-center mb-2 relative z-10"
  >
    Still Have Questions?
  </h3>

  <p class="text-muted-foreground text-center mb-4 relative z-10">
    Our AI experts are here to help you understand how artificial intelligence
    can transform your business operations and drive growth.
  </p>

  <div class="grid grid-cols-2 gap-4 w-full text-center relative z-10">
    <div
      class="bg-card/50 backdrop-blur-xl border border-primary-foreground/10 p-4 rounded-xl flex flex-col items-center"
    >
      <div class="text-2xl sm:text-3xl font-bold">&lt; 2hrs</div>
      <div class="text-xs text-muted-foreground">Response Time</div>
    </div>
    <div
      class="bg-card/50 backdrop-blur-xl border border-primary-foreground/10 p-4 rounded-xl flex flex-col items-center"
    >
      <div class="text-2xl sm:text-3xl font-bold">100%</div>
      <div class="text-xs text-muted-foreground">Free Consultation</div>
    </div>
  </div>
</div>
